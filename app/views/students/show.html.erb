<div><h1><a href='<%= "/students/#{@student.id}" %>'><%= @student.to_s %></a></h1></div>

<% if @student.active %>
  This student is currently active.
<% else %>
  This student is currently inactive.
<% end %>
